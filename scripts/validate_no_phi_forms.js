const fs=require('fs');const path=require('path');const {root,fail}=require('./validator_utils');const errs=[];function walk(d){for(const f of fs.readdirSync(d)){const p=path.join(d,f);const st=fs.statSync(p);if(st.isDirectory())walk(p);else if(f.endsWith('.html')){const s=fs.readFileSync(p,'utf8');if(/<textarea/i.test(s))errs.push(`Textarea found in ${p}`);if(/type="file"/i.test(s))errs.push(`File upload found in ${p}`);if(/symptom|diagnosis|medical history|insurance number|social security/i.test(s)&&/<form/i.test(s))errs.push(`Possible PHI form found in ${p}`);}}}walk(path.join(root,'dist'));fail(errs);
