const fs=require('fs');const path=require('path');const {root,fail,allContent}=require('./validator_utils');const errs=[];function due(d){return new Date(d)<=new Date(process.env.BUILD_DATE||new Date().toISOString().slice(0,10));}function pub(item){return item.meta.status==='published'||(item.meta.status==='approved'&&due(item.meta.scheduledAt));}
for(const item of allContent()){const folder=item.meta.type==='quarterly_white_paper'?'white-papers':'insights';const publicOut=path.join(root,'dist','resources',folder,item.meta.slug,'index.html');const previewOut=path.join(root,'dist','previews','resources',folder,item.meta.slug,'index.html');if(!fs.existsSync(previewOut))errs.push(`Missing admin preview page: ${item.meta.id}`);if(!pub(item)&&fs.existsSync(publicOut))errs.push(`Unapproved or not-yet-due content rendered publicly: ${item.meta.id}`);if(pub(item)&&!fs.existsSync(publicOut))errs.push(`Public content missing rendered page: ${item.meta.id}`);}fail(errs);
